spring:
  application:
    name: passport
  profiles:
    include: secret
  datasource:
    url: jdbc:mysql://localhost:3306/mcm_passport
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
